# RTK

Use `rtk` for verbose shell commands so command output remains compact.

Examples:

```bash
rtk git status --short
rtk git diff
rtk rg "pattern"
rtk npm test
```

If a command is not specially supported, `rtk` passes it through.

